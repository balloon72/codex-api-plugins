interface:
  display_name: "GitLab"
  short_description: "Inspect projects, merge requests, discussions, and issues"
  icon_small: "./assets/gitlab-small.svg"
  icon_large: "./assets/gitlab.svg"
  default_prompt: "Use $gitlab to inspect a GitLab merge request, issue, wiki page, or project, summarize the current state, and route to the right GitLab workflow."
